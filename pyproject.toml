[project]
name = "vespa"
version = "0.1.0"
description = "The SuperWASP Variable Star Photometry Archive."
requires-python = ">=3.10,<3.11"
dependencies = [
    "django>=5.2.15,<6",
    "gunicorn",
    "psycopg2",
    "astropy",
    "humanize",
    "celery",
    "pyyaml",
    "django-celery-results",
    "seaborn",
    "pillow",
    "sentry-sdk",
    "ujson",
    "markdown",
    "panoptes-client",
    "pandas",
    "numpy>=2.0",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
    "pytest-pspec",
    "pytest-django",
    "pytest-mock",
    "pytest-pythonpath",
    "pylint",
]

[tool.uv]
package = false
